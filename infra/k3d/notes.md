# Notas do Cluster Local BrioIT

- Cluster criado com k3d v5.x
- Persistência do PostgreSQL com local-path StorageClass (automático k3d)
- Ingress padrão: Traefik
- TLS via cert-manager (Let's Encrypt staging)
